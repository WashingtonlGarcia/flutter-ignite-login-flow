name: flutter_ignite_login_flow
description: A new Flutter project.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  fluro: ^2.0.3
  flutter_bloc: ^7.0.1
  firebase_auth: ^3.0.1
  firebase_core: ^1.4.0
  dartz: ^0.10.0-nullsafety.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.3


flutter:
  uses-material-design: true
